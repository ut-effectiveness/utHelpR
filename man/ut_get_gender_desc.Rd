% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_manipulation_helpers.R
\name{ut_get_gender_desc}
\alias{ut_get_gender_desc}
\title{Get Gender Description}
\usage{
ut_get_gender_desc(.data, assign_gender = FALSE)
}
\arguments{
\item{.data}{A data frame containing the \code{student_id} and \code{gender_code} columns.}

\item{assign_gender}{A logical value indicating whether to assign gender codes based on the last digit of the \code{student_id}. Default is \code{FALSE}.}
}
\value{
A data frame with an additional \code{gender_desc} column that contains the gender description ("Female", "Male", or "Unspecified").
}
\description{
This function processes a data frame to assign gender codes based on the last digit of the \code{student_id} and provides a descriptive gender label. It is particularly useful for datasets where gender information might be incomplete or needs to be inferred.
}
\details{
If \code{assign_gender} is \code{TRUE}, the function assigns gender codes based on the last digit of the \code{student_id}. If the last digit is even, the gender code is assigned as "F" (Female); if odd, it is assigned as "M" (Male). If \code{assign_gender} is \code{FALSE}, the function uses the existing \code{gender_code} column.
}
\examples{
\dontrun{
# Example usage:
df <- data.frame(student_id = c("00123456", "00234567", "00345678"), gender_code = c("F", "M", NA))
get_gender_desc(df, assign_gender = TRUE)
}

}
